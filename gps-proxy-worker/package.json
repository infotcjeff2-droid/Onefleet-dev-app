{
  "name": "fleet-gps-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --outdir=dist --platform=browser",
    "deploy": "npm run build && wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260814.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.123.0"
  }
}
